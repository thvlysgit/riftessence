# Example environment variables (do NOT commit real secrets)
# Copy to .env and fill values locally
RIOT_API_KEY=
# Postgres DATABASE_URL format examples:
# postgres (basic): postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# postgres with schema and ssl: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public&sslmode=require
DATABASE_URL=
PORT=3333
# Discord webhook for user reports
DISCORD_REPORT_WEBHOOK=
